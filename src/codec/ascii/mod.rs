//! ASCII biological sequence alphabets

use std::fmt;

use crate::codec::Codec;
//use bio_seq_derive::Codec;

pub use crate::codec::ascii::iupac;

#[cfg(test)]
mod tests {
    #[test]
    fn string_to_seq() {
        true
    }
}
